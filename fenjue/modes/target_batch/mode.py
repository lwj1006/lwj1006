LABEL = "target fixed prompt batch"

def activate(batch, args=None) -> None:
    print("Prompt mode D active: target image batch. Images are read from ../target and moved to ../complete.", flush=True)
